# Herdlite PHP Runtime

PHP-side runtime loaded by the Herdlite extension/bootstrap hook.

This package will eventually contain Laravel integration such as:

- dump/dd capture
- query listeners
- log listeners
- request grouping

This is deferred until local hosting is working.
